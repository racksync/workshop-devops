apiVersion: v1
kind: Namespace
metadata:
  name: statefulset-demo
  labels:
    purpose: workshop
    topic: statefulset
  annotations:
    description: "Namespace สำหรับการทดลอง Kubernetes StatefulSet"
    created-by: "DevOps Workshop"
# Namespace เป็น logical boundary ในคลัสเตอร์ ช่วยแยกทรัพยากรและป้องกันการชนกันของชื่อ
# เป็นพื้นฐานสำหรับการแยกสภาพแวดล้อมหรือทีมในการทำงานบน Kubernetes
